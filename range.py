# we can generate a sequence of number using range() function.range(10) will generate number from 0 to 9 (10 number)
# we can also define the start ,stop,and step size as range(start,stop,step.size).step size defaults to 1 if not provided.

a=list(range(1,20,2))

print(a)